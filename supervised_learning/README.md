# Supervised Learning

This directory contains supervised learning projects, including neural network and classification models.

- `classification/` — Binary and multiclass classification models

See the `classification/README.md` for more details on classification tasks.
